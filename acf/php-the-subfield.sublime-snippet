<snippet>
    <content>
<![CDATA[if( \$${1:field_name} = get_sub_field('${1:field_name}') ) {
    if (is_string(\$${1:field_name})) {
    	echo '\$${1:field_name}: ' . \$${1:field_name};
    }
    else {
    	echo '<pre>\$${1:field_name}: '; var_dump(\$${1:field_name}); echo '</pre>';
    }    
}]]>
    </content>
    <tabTrigger>subfield</tabTrigger>
    <description>Display ACF subfield</description>
    <scope>source.php</scope>
</snippet>